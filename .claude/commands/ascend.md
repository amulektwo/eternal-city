---
description: One visual ascent pass on the Eternal City — see, compare, improve, verify, keep only if better
---
You are running ONE ascent pass on THE ETERNAL CITY (this repo).

MOTIVATIONAL INTENT — never lose this, it overrides everything: the city
must produce the FEELING of the reference images in /reference/ — white
marble, living gold, perpetual golden-hour glory, GLORY NOT GLOOM — while
holding 50+ FPS desktop / 25+ mobile. Beauty that breaks the frame rate
fails. Speed that stays ugly fails. Where a board conflicts with written law
(the boards' red fruit vs the white-gold zero-red law), THE LAW WINS — read
/reference/README.md and /STATE.md LAWS-LEARNED before judging any gap.

THE PASS:
1. READ /STATE.md — inherit what previous passes learned. Obey every entry
   under LAWS-LEARNED. Read /reference/README.md for what each board teaches.
2. SEE: open the build and screenshot these vantages: Gate Plaza, Street of
   Gold, Fountain Court, Tree of Life Plaza, Great Sanctuary, Throne Hall,
   Waterfall Terrace.
3. COMPARE each against /reference/. Name the 3 biggest gaps in FEELING
   (materials? light? ornament? atmosphere? life?). Write them down before
   touching code.
4. FIX the 3 gaps. Treat the current build as a 100 index — land at 120+.
   ZOOM OUT LAW: if a gap survived two passes (check STATE.md), do NOT patch
   again — diagnose the underlying system (material rig, lighting rig, post
   stack, geometry) and fix the architecture. Never break a standing
   LAW-LEARNED to chase a board.
5. VERIFY: re-screenshot the same vantages, compare against previous AND
   /reference/. Zero console errors. FPS 50+ desktop every area (report
   numbers). Keep changes ONLY if improvement is obvious; else git revert
   and say so.
6. ENCODE: if you fixed a recurring class of problem, write the rule into
   /STATE.md under LAWS-LEARNED.
7. UPDATE /STATE.md: current state, last pass, weaknesses re-ranked, FPS.
8. If kept: git add -A && git commit -m "Ascent pass — <one line>" && git
   push. If reverted: log why in STATE.md, commit the ledger only.

$ARGUMENTS (optional): a specific area or gap to focus this pass on.
